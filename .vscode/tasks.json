{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run SASS",
            "type": "shell",
            "command": "sass --watch scss:css --no-cache",
            "windows": {
                "command": "sass --watch scss:css --no-cache"
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}